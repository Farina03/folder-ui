import React from 'react'

const BlankPage = () => {
  return (
    <div>BlankPage</div>
  )
}

export default BlankPage

// // console.log(tempObj[deletedFolderParent].child, "child")
    //setFolders(tempObj)
    
    // Object.keys(tempObj).map(item => {
    //     let temp = tempObj[item]
    //     console.log(temp, item.id, "temp")
    //     if(item.id !== alertTrigger) {
            
    //     }
    // })
    //console.log(tempObj, "tempobj")
    // console.log(Object.keys(tempObj).filter(item => item !== alertTrigger))
    //         .reduce((obj, item) => {
    //             obj[item] = tempObj[item]
    //             return tempObj
    //         })
    //     {
    //     if(item !== alertTrigger) {
    //         tempObj = {
    //             [item] : folders[item]
    //         }   
    //     }
    //     setFolders(tempObj)
    //     console.log(tempObj, "this")
    // })